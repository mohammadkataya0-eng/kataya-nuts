<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Kataya Nuts - Home</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-[#b51916] text-white font-sans min-h-screen flex flex-col relative">

  <!-- Navbar -->
  <nav class="relative flex justify-center items-center py-6 text-lg font-medium uppercase tracking-wide bg-[#b51916]">
    <!-- Centered Nav Links -->
    <div class="flex gap-10">
      <a href="/" class="hover:text-[#d89a63] transition">Home</a>
      <a href="/bag" class="hover:text-[#d89a63] transition">Your Bag</a>
      <a href="/checkout" class="hover:text-[#d89a63] transition">Checkout</a>
    </div>

    <!-- Hamburger Menu -->
    <div class="absolute right-6 top-1/2 -translate-y-1/2">
      <button onclick="toggleLoginMenu()" class="text-white text-2xl hover:text-[#d89a63] transition">
        &#9776;
      </button>
    </div>
  </nav>

  <!-- Hidden Login Dropdown -->
  <div id="loginMenu" class="hidden absolute right-6 top-20 bg-white text-[#b51916] rounded-xl shadow-lg p-4 z-50">
    <a href="/admin-login" class="block px-4 py-2 font-semibold text-sm rounded-full bg-[#b51916] text-white hover:bg-[#d89a63] text-center transition">
      🔐 Admin Login
    </a>
  </div>

  <!-- Hero Section -->
  <main class="flex flex-col items-center justify-center flex-1 text-center px-6 py-16">
    <img src="/images/logo.png" alt="Kataya Nuts Logo" class="w-40 mb-6 drop-shadow-lg" />

    <h1 class="text-4xl md:text-5xl font-bold mb-4">Welcome to Kataya Nuts</h1>
    <p class="text-lg md:text-xl text-white/90 max-w-2xl">
      Kataya Nuts is your destination for premium roasted and raw nuts, handpicked and blended just for you. 
      Fill your own custom bags and send your order directly to WhatsApp — the experience is easy, fast, and delicious.
    </p>

    <a href="/bag" class="mt-10 inline-block bg-white text-[#b51916] hover:bg-[#d89a63] hover:text-white px-8 py-3 rounded-full font-semibold text-lg transition">
      🛍️ Start Creating Your Bag
    </a>
  </main>

  <!-- Footer -->
  <footer class="text-center py-4 text-white/70 text-sm">
    &copy; <%= new Date().getFullYear() %> Kataya Nuts. All rights reserved.
  </footer>

  <script>
    function toggleLoginMenu() {
      const menu = document.getElementById("loginMenu");
      menu.classList.toggle("hidden");
    }

    // Hide the menu if clicked outside
    window.addEventListener('click', function (e) {
      const menu = document.getElementById("loginMenu");
      const button = document.querySelector('button[onclick="toggleLoginMenu()"]');
      if (!menu.contains(e.target) && !button.contains(e.target)) {
        menu.classList.add("hidden");
      }
    });
  </script>

</body>
</html>
